---
name: "horror-master"
description: >
  恐怖大师——用认知机制驱动的恐怖叙事生成工具。
  核心假设：恐怖片的编剧逻辑与其他类型不同，它靠的是认知层面的操控（不安、错位、边界坍塌），而不是人物弧光或情节反转。
  主流程：需求确认 → 灵感启动（可选）→ 概念选配 → 结构设计 → 大纲 → 场景展开 → 自检 → 交付 → 沉淀（可选）。
  触发词：「恐怖大师」「恐怖片编剧」「恐怖片剧本」「horror」「写恐怖片」「恐怖短片」「鬼故事」
  当用户提到恐怖叙事相关话题时，即使没有明确说出「恐怖大师」，也应考虑使用此 skill。

# 恐怖大师 · 认知驱动恐怖叙事工具

## 这是什么

一个基于认知科学概念的恐怖片/短篇/鬼故事生成工具。它不做"教你写恐怖片"的教程——它是一套**操作工程**：

- **concepts.md** 告诉你"为什么这个操作能制造恐惧"（理论层）
- **patterns/** 告诉你"别人用过什么模式、具体怎么操作"（方法层）
- **craft-checklist.md** 帮你检查"写完的东西有没有问题"（检验层）
- **主流程**把以上三者串起来，从你的想法/种子出发，一步步产出完整剧本

## 核心资源

### 必读（每次调用都加载）

- **概念层**：`references/concepts.md` — 十三条脉络，114个概念，每个有编剧操作映射
- **路由索引**：`references/patterns.md` — 模块地图 + 快速导航表

### 按需加载（根据当前步骤定位到具体文件）


| 当前在做什么           | 读哪个文件                                               |
| ---------------- | --------------------------------------------------- |
| 设计节奏 / 检验张力分布    | `patterns/pacing.md`                                |
| 设计信息差 / 选定反转类型   | `patterns/information.md`                           |
| 设计角色 / 威胁 / 开头结尾 | `patterns/character.md`                             |
| 找场景 / 身体 / 社会元素  | `patterns/elements.md`                              |
| 确认子类型框架          | `patterns/subgenres.md`                             |
| 检查俗套 / 找反套路灵感    | `patterns/tropes-anti.md`                           |
| 写完后自检            | `craft-checklist.md`                                |
| 需要灵感种子           | `reddit/twosentence.md` 或 `reddit/nosleep.md`（英文素材）或 `tianya/lianpeng.md`（中文素材·莲蓬鬼话经典库） |


### 外部依赖

- 概念库（通用概念页）：`/Users/myke/Documents/MERJIC/概念库/概念页/`
- concept-studio 路径：`/Users/myke/Documents/MERJIC/个人总部/skills/concept-studio/`

---

## 主流程

这是本 skill 的核心。所有调用最终都走这条线——区别只在于从哪一步进入。

```
Step 0  确认篇幅和格式
Step 1  概念选配（从十三条脉络选主线）
Step 2  结构设计（读 patterns/ 对应模块）
Step 3  大纲（每场戏摘要 + 视觉化检验）
Step 4  场景展开（逐场写动作和对白）
Step 5  自检（craft-checklist + 俗套检查 + 节奏验证）
Step 6  交付 + 沉淀判断（可选）
```

---

### Step 0：确认篇幅和格式


| 篇幅        | 时长/字数             | 格式             | 结构参考                                          |
| --------- | ----------------- | -------------- | --------------------------------------------- |
| 长片        | 90-120分钟          | 标准编剧格式         | `pacing.md` 三幕 + `information.md` 幕间切换        |
| 短片        | 10-30分钟           | 文学性剧本          | `pacing.md` 单一相变 + `character.md` 开篇结尾        |
| **5分钟短片** | **~1500字 / 5页剧本** | **极简 beat 结构** | `**information.md` 反转类型 + `pacing.md` 5分钟网格** |
| 鬼故事       | 500-3000字         | 极简叙事           | `information.md` 延迟揭露 + `character.md` 结尾策略   |


用户没指定时，默认短片。

---

### Step 0.5：灵感启动（可选）

用户没有给出具体点子时，从素材池提取种子：

1. **5分钟/鬼故事** → 从 `reddit/twosentence.md` 按知情权模式筛选或随机抽一条做种子；中文语境优先从 `tianya/lianpeng.md` 选取（搭车父子/一封家书/姑妄听之）
2. **中等篇幅** → 从 `reddit/nosleep.md` top 30 的开头分析开篇模式做起点；或从 `tianya/lianpeng.md` 选中篇素材（电影院六年/左央/深山孤庙）
3. **长片/系列** → 从 `tianya/lianpeng.md` 长篇神作中拆解世界观和入局模式（青囊尸衣/宜昌鬼事/双鱼玉佩）
4. 有具体方向但需要填充 → 跳过，直接进 Step 1

确定种子后告诉用户方向选项，等确认再往下走。

---

### Step 1：概念选配

根据用户给的方向/素材/想法，从 concepts.md 的十三条脉络中选 **2-4 条主线**。

选配逻辑：

- 用户的想法天然靠近哪条脉络？→ 优先选
- 哪条跟主线互补？→ 选 1-2 条做副线
- 避免只选一条（扁平）和选太多（散）

输出选配方案，**等用户确认后再进 Step 2**。

---

### Step 2：结构设计

按路由索引定位到具体模块文件来设计：


| 设计任务  | 读哪个文件                     | 输出什么                 |
| ----- | ------------------------- | -------------------- |
| 节奏骨架  | `patterns/pacing.md`      | 硬停点位置、节奏形态选择         |
| 信息操作  | `patterns/information.md` | 知情权指纹（基础位置+动力学）、反转类型 |
| 角色/威胁 | `patterns/character.md`   | 主角设定、威胁本质、主角驱动检验     |
| 开篇/结尾 | `patterns/character.md`   | 开头策略、结尾方向            |
| 元素填充  | `patterns/elements.md`    | 场景空间、身体恐怖类型、社会背景     |


**篇幅对应的结构模板：**


| 篇幅        | 结构要点                                                       |
| --------- | ---------------------------------------------------------- |
| 长片        | 三幕：建置（日常→裂痕）→ 对抗（崩塌→认知失效）→ 解决（压抑回归/开放/崇高）。每幕 30-40min      |
| 短片        | 单一情境+一次相变：日常→临界点→崩塌。10-30min                               |
| **5分钟短片** | **3-5 个 beat，每个 = 动作+反应+意义转变。选一种信息不对称主打。整部片子 = 一个 Teaser** |
| 鬼故事       | 一个锚点+一个留白：正常→不对劲→不闭合                                       |


输出结构大纲（幕/段划分、核心概念、转折点、信息分配策略），**等用户确认后进 Step 3**。

---

### Step 3：大纲

结构确认后输出完整大纲：

```
场景[N]：[地点] / [时间]
摘要：[一句话]
视觉化检验：[关键信息能否用一帧画面传达？不能则标注 ⚠️]
概念背景：[这场激活的认知机制，仅标注]
```

**视觉化检验原则**：
- 每个关键恐怖信息必须能被**一帧画面 + 声音设计**传达，不依赖阅读文字
- 纯听觉/心理层面的恐怖必须找到视觉替代（「脚步声」→「天花板震动」，「内心不安」→「手指无意识反复搓同一处」）
- 如果一场戏的恐惧核心是「主角看到了某段文字/消息/记录」，这场戏不合格——重做信息载体
- 标注 ⚠️ 的场景必须在 Step 4 找到视觉解决方案，否则删除或合并

输出全部场景摘要 + 张力曲线示意，**等用户确认后进 Step 4**。

---

### Step 4：场景展开

逐场写：

- 场景目标（要让观众感到什么？）
- 动作和对白
- 节奏标记（张力↑ / 持稳 / 释放↓）

概念机制作为参考背景自然融入，不强求每场激活某个概念，但要有意识。

---

### Step 5：自检

写完后逐项过：

1. **概念覆盖度**：主线脉络是否都有至少一个场景深度激活？
2. **俗套检查**：角色反应是否类型标配？威胁是否单概念？结局是否默认选项？（对照 `tropes-anti.md`）
3. **节奏检查**：张力曲线是否有起伏？（对照 `pacing.md` 的节奏形态）
4. **篇幅检查**：字数/时长是否在目标范围？
5. **影像化硬检**：
   - 逐场过：每场的关键恐惧信息，关掉声音、遮住字幕（假设有），仅看画面能否接收到？不能则重写
   - 对白依赖度：删掉所有对白，仅靠画面+表演+音效，故事是否可理解？如果理解度低于 70%，对白过重
   - 视觉单调性：是否有连续两场以上使用相同机位/相同景别/同一空间无变化？如果有，必须加视觉变量
   - 信息载体检验：是否存在「观众需要阅读才能获取的关键信息」？有则必须替换为视觉载体
6. **Craft 自检**：读 `craft-checklist.md`，逐条过开放性问题集

发现问题直接改，修改记录附在剧本末尾。

---

### Step 6：交付 + 沉淀判断

交付剧本给用户。然后执行沉淀扫描（**可选，每次问用户**）：

#### 6a. 片段沉淀

扫描生成内容，找出独立可复用的组件：

- 特别好的 beat → 可追加到 `patterns/information.md` 对应反转类型的示例区
- 有效的开篇段落 → 可追加到 `patterns/character.md`
- 好的结尾设计 → 可追加到 `patterns/character.md`

问用户：「这个片段我觉得不错，要存到参考库里吗？」

#### 6b. 组合记录

这次用的概念组合以前用过吗？没有则记录到 `references/generated-combinations.md`：

```markdown
## [日期] [篇幅] [题材关键词]

**组合**：[基础位置] + [动力学/反转类型] + [节奏形态]
**概念主线**：[用了哪些脉络]
**用户反馈**：[有效/一般/需调整]
**备注**：[什么效果好、什么不行、下次怎么调]
```

#### 6c. 用户原创点子

用户在使用过程中自己贡献的点子/设定，记录到 `references/generated-ideas.md`：

```markdown
## [日期]

**原始想法**：[用户的原话或描述]
**展开方向**：[实际走了哪个方向]
**用到哪些概念**：[脉络列表]
**状态**：已使用 / 待展开 / 存档备用
```

---

## 快速入口（非主流程的 shortcut）

以下情况可以跳过主流程的部分步骤，直接切入：


| 用户说           | 从哪步进入                        | 做什么            |
| ------------- | ---------------------------- | -------------- |
| 「查概念」「XX是什么」  | 直接读 `concepts.md`            | 返回定义+编剧操作+所属脉络 |
| 「查套路」「XX 怎么用」 | 先读 `patterns.md` 路由索引，再读对应模块 | 返回操作方法+经典用例+风险 |
| 「诊断一下这段」      | 给出素材 → 跳到 Step 5 自检逻辑        | 扫描认知覆盖度，标盲区和浅层 |
| 「改一下这里」       | 定位到对应场景 → 重跑 Step 4 该场景      | 改写+标注改动原因      |
| 「全貌」「有哪些概念」   | 读 `concepts.md`              | 输出十三条脉络结构图     |


---

## 约束

- 概念层是方向感，不是硬约束——生成时概念提供指引，写戏靠对情境和人物的理解
- **影像优先（硬约束）**：本 skill 的产出默认格式为影像剧本（短片/长片），所有恐怖信息必须可通过画面+声音传达。纯文字/阅读才能获取的信息（手机屏幕内容、文档文字、内心独白）不作为主要恐惧载体。如果关键信息只能通过「读」才能获得，这个设计不合格，必须重做
- **视觉化检验标准**：
  - 关键恐怖信息 = 一帧画面 + 可选的声音设计就能让观众接收到（不需要字幕、不需要暂停、不需要凑近看）
  - 心理层面的恐惧必须有物理对应物：「不安」→ 可被看到的微动作/环境变化；「记忆异常」→ 物理证据的视觉差异；「认知崩溃」→ 行为或空间的断裂
  - 对白是辅助，不是信息主力。如果删掉对白观众就看不懂剧情，对白比重过大
  - 单一空间内必须有机位/光影/演员走位/道具状态的变化来制造视觉节奏，不能变成"固定镜头看人表演"
- 编剧操作建议必须标注俗套化风险——每个"可以这样写"配套"什么时候会变俗套"
- 概念补充和沉淀由 concept-studio 负责，本 skill 不建独立概念库
- 如果素材不属于恐怖片，不强行套框架——坦诚说明超出范围
- **沉淀永远可选、永远经用户确认**——不自动存储任何生成内容

---

## 文件总览

```
skills/horror-master/
├── SKILL.md                          ← 你在这里
├── references/
│   ├── concepts.md                   ← 为什么（认知机制原理）
│   ├── patterns.md                   ← 路由索引（模块地图）
│   ├── craft-checklist.md            ← 检验（自检问题集）
│   ├── patterns/                     ← 怎么用（6个模块）
│   │   ├── pacing.md                 ←   叙事节奏 + 硬停点 + 节奏形态
│   │   ├── information.md           ←   知情权 + 反转类型目录（最核心）
│   │   ├── character.md             ←   角色 + 威胁 + 开篇结尾
│   │   ├── elements.md              ←   场景/身体/社会元素库
│   │   ├── subgenres.md             ←   子类型差异
│   │   └── tropes-anti.md           ←   Tropes速查 + 反套路
│   ├── reddit/                       ← 英文本地素材（不随 skill 分发）
│   │   ├── twosentence.md           ←   100条两句话恐怖故事
│   │   └── nosleep.md               ←   nosleep top 30 全文 + 索引
│   ├── tianya/                       ← 中文本地素材（不随 skill 分发）
│   │   └── lianpeng.md              ←   莲蓬鬼话经典库：12部长篇+16部短篇神贴+叙事DNA+认知机制索引
│   ├── generated-combinations.md    ← 沉淀：有效组合记录（自增长）
│   └── generated-ideas.md            ← 沉淀：用户原创点子（自增长）
```

&nbsp;